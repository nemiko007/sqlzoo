# 1984年(化学と物理を最後に)

**テーブル構造:** `nobel(yr, subject, winner)`

```sql
SELECT winner, subject FROM nobel 
WHERE yr=1984 
ORDER BY CASE WHEN subject IN ('physics', 'chemistry') THEN 1 ELSE 0 END, 
         subject, winner
```
