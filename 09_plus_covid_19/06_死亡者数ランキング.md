# 死亡者数ランキング

**テーブル構造:** `covid(name, whn, confirmed, deaths, recovered)`

2020年4月20日時点の感染者数順位と、死亡者数順位を表示
```sql
SELECT name, confirmed, RANK() OVER (ORDER BY confirmed DESC) rc, deaths, RANK() OVER (ORDER BY deaths DESC) rd FROM covid 
WHERE whn = '2020-04-20' 
ORDER BY confirmed DESC
```
